{
  "buildCommand": "bun build index.html index-br.html --outdir=dist && cp -r public/* dist/",
  "outputDirectory": "dist",
  "installCommand": "bun install",
  "rewrites": [
    { "source": "/br", "destination": "/index-br.html" }
  ]
}
